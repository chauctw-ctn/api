{
  "name": "my-monorepo-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/fetch"
  ],
"scripts": {
  "dev:api": "npm run dev -w api",
  "dev:fetch": "npm run dev -w fetch",
  "start:api": "npm start -w api",
  "start:fetch": "npm start -w fetch",
  "dev": "node app.js"
},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}